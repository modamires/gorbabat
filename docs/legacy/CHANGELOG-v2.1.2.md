# Changelog

تمام تغییرات مهم پروژه گوربابات در این فایل ثبت می‌شوند.

فرمت کلی بر اساس نسخه‌بندی ساده پروژه است.

---

## [v2.1.2] - 2026-08-22

### 🤖 AI Chatbot

- مدل اصلی چت‌بات به `@cf/qwen/qwen3-30b-a3b-fp8` ارتقا یافت.
- مدل `@cf/zai-org/glm-4.7-flash` به عنوان fallback اضافه شد.
- برای Qwen3 حالت thinking در چت کوتاه با `/no_think` غیرفعال شد تا خروجی نهایی خالی نماند.
- سقف خروجی AI به 256 توکن تنظیم شد.
- parser خروجی Workers AI برای چند شکل پاسخ مختلف مقاوم‌تر شد.
- پاک‌سازی تگ‌های `<think>` و `<reasoning>` به خروجی اضافه شد.
- در صورت شکست مدل اصلی، fallback خودکار اجرا می‌شود.

### 💬 تجربه چت

- پیام‌های عادی خصوصی که دستور یا Easter Egg نیستند وارد چت گوربابات می‌شوند.
- پاسخ اولیه به صورت تعداد تصادفی «میو» نمایش داده می‌شود.
- دکمه Inline با عنوان `ترجمه به زبون آدمیزاد` اضافه شد.
- هنگام پردازش AI پیام موقت ترجمه نمایش داده می‌شود.
- `sendChatAction: typing` هنگام تولید پاسخ فعال می‌شود.
- جلوگیری موقت از دابل‌کلیک و اجرای هم‌زمان AI برای یک پیام اضافه شد.
- در خطای AI، دکمه `دوباره ترجمه کن` نمایش داده می‌شود.

### 😾 شخصیت گوربابات

- System Prompt کامل‌تر و دقیق‌تر شد.
- شخصیت گوربابات به شکل گربه خیابانی، لجباز، گستاخ، شرور، کم‌حرف و حاضر‌جواب تعریف شد.
- شوخی‌های زن‌باره و شیطنت‌آمیز در حد لحن و متلک کوتاه اضافه شد.
- به مدل تأکید شد شخصیت روی لحن اثر بگذارد و نباید باعث جواب بی‌ربط یا اطلاعات ساختگی شود.
- دانش داخلی درباره سازنده، قابلیت‌ها، استریک، پیام ناشناس، Easter Eggها و اتفاق‌های مخفی اضافه شد.
- لحن فارسی محاوره‌ای ایرانی در prompt تقویت شد.

### 🥚 Easter Eggها

Easter Eggهای متنی جدید و مسیرهای مخفی اضافه شدند، از جمله:

- `pspsps`
- ماهی
- جعبه
- لیزر
- کنسرو
- دامپزشک
- نه‌جان
- «من گربه‌ام»
- 404
- نخ / کاموا
- کت‌نیپ
- خیار
- کیبورد
- پنجه
- صدا زدن دقیق «گوربابات»
- مسیر مخفی «پیش» نصفه

چند Achievement مخفی جدید نیز به این مسیرها متصل شدند.

### 🎲 Chance Events

اتفاق‌های نادر بیشتری به «پیش پیش» اضافه شد:

- گربه نامرئی
- گربه مدیرعامل
- گربه موقت
- گربه لجندری
- اردک اشتباهی
- قهر گربه
- گربه اضافه
- اعتراض پس از اسپم
- سکوت اعتراضی

### 🎙️ Voice

- تشخیص ویس فارسی با `@cf/openai/whisper-large-v3-turbo` حفظ شد.
- Prompt تشخیص ویس برای عبارت «پیش پیش» تنظیم شده است.
- در صورت نبودن Workers AI Binding، قابلیت ویس fail-safe می‌شود و کل ربات از کار نمی‌افتد.

### ⚡ KV & Performance

- مشکل write شدن پروفایل کاربر با هر پیام برطرف شد.
- پروفایل کاربر فقط در شرایط لازم یا حداکثر تقریباً روزی یک‌بار ذخیره می‌شود.
- `/start` تکراری دیگر آمار starter را بی‌دلیل write نمی‌کند.
- Burst کوتاه‌مدت «پیش پیش» از KV به حافظه Worker منتقل شد.
- state مربوط به «پیش» نصفه از KV به حافظه Worker منتقل شد.
- آمار گربه‌ها cache و با فاصله زمانی flush می‌شود.
- refresh نام و username کاربران برای پنل مدیر محدودتر شد.
- `/health` دیگر برای تست KV عملیات write/delete انجام نمی‌دهد.
- wrapperهای `safeKvGet`, `safeKvPut` و `safeKvDelete` اضافه شدند.
- خطاهای KV به جای متوقف کردن کل مسیر تا جای ممکن log و تحمل می‌شوند.
- حذف چندکلیدی کاربران غیرفعال با tombstone تک‌کلیدی جایگزین شد.
- Mapهای حافظه دارای محدودیت اندازه و pruning شدند.

### 🛡 Reliability

- logging خطاها با `name`, `message`, `stack` و `cause` دقیق‌تر شد.
- خطاهای `rememberPrivateUser` دیگر نباید کل پردازش پیام خصوصی را متوقف کنند.
- خروجی مدل AI در چند فرمت مختلف قابل استخراج است.
- در شکست ویرایش پیام AI، پاسخ به صورت پیام جدید ارسال می‌شود.
- کاربران بلاک‌کننده از لیست ارسال‌های بعدی کنار گذاشته می‌شوند.

### 👋 Welcome & Help

- پیام `/start` بازنویسی شد.
- قابلیت چت با گوربابات به پیام خوش‌آمدگویی اضافه شد.
- راهنمای `/help` برای چت، ترجمه، استریک، آمار، پیام ناشناس و میو روزانه به‌روز شد.

---

## [v2.1.1]

### Fixed

- سازگاری parser پاسخ Workers AI با خروجی OpenAI-style بهتر شد.
- استخراج پاسخ از `choices[0].message.content` و چند شکل جایگزین اضافه شد.
- logging شکل خروجی AI برای عیب‌یابی بهتر شد.

---

## [v2.1.0]

### Changed

- مدل چت از Llama 3.2 3B به Qwen3 30B منتقل شد.
- Prompt شخصیت گوربابات بازنویسی و لحن فارسی تقویت شد.
- پیام خوش‌آمدگویی `/start` به‌روزرسانی شد.
- پارامترهای تولید متن برای پاسخ‌های کوتاه‌تر و کنترل‌شده‌تر تنظیم شدند.

---

## [v2.0.0]

### Added

- چت‌بات گوربابات اضافه شد.
- پاسخ اولیه «میو» با تعداد تصادفی اضافه شد.
- دکمه «ترجمه به زبون آدمیزاد» اضافه شد.
- Workers AI برای پاسخ فارسی به متن عادی اضافه شد.
- پیام موقت هنگام پردازش AI اضافه شد.
- Easter Eggها و اتفاق‌های مخفی توسعه پیدا کردند.

### Performance

- معماری KV برای کاهش writeهای روزانه بازطراحی شد.
- بخش‌هایی از state کوتاه‌مدت به حافظه Worker منتقل شدند.
- خطای KV از مسیر حیاتی ربات جدا شد.

---

## نسخه‌های قبل

نسخه‌های قدیمی شامل هسته اولیه قابلیت‌های زیر بودند:

- ارسال عکس گربه با «پیش پیش»
- تشخیص «پیش پیش» در ویس
- میو روزانه
- استریک
- آمار
- پیام ناشناس و Reply دوطرفه
- Broadcast مدیریتی
- Cron
- Achievementها
- Chance Eventهای گربه‌ای

جزئیات نسخه‌های قدیمی‌تر در این Changelog ثبت نشده‌اند.
